services:
    php:
        build: .
        container_name: e_radiologie_backend_php
        ports:
            - "18740:8000"
        volumes:
            - .:/app
        working_dir: /app
        depends_on:
            - database
        command: php -S 0.0.0.0:8000 -t public

    database:
        image: postgres:16
        container_name: e_radiologie_backend_postgres
        ports:
            - "15432:5432"
        environment:
            POSTGRES_DB: ${POSTGRES_DB}
            POSTGRES_USER: ${POSTGRES_USER}
            POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
        volumes:
            - e_radiologie_backend_postgres_data:/var/lib/postgresql/data

volumes:
    e_radiologie_backend_postgres_data:
